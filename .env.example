# ===== Reddit Search Backend =====
# REDDIT_BACKEND=auto       # auto (default) | openai | brave
# auto: uses brave if BRAVE_API_KEY set, else openai if OPENAI_API_KEY set

# Brave mode (recommended -- free, no LLM needed)
# BRAVE_API_KEY=            # Get free key at https://brave.com/search/api/

# OpenAI mode (original -- requires Responses API access)
# OPENAI_API_KEY=sk-...
# OPENAI_API_BASE=          # Optional: custom API base URL (e.g., http://localhost:38801/v1)

# ===== X/Twitter Search =====
# Cookie auth (grab from browser DevTools -> Application -> Cookies -> x.com)
# AUTH_TOKEN=
# CT0=
# XAI_API_KEY=              # Optional fallback if cookie auth fails

# ===== Web Search (optional, adds web as 4th source) =====
# BRAVE_API_KEY=            # Same key as above, also used for --include-web
# PARALLEL_API_KEY=         # Parallel AI (preferred web search)
# OPENROUTER_API_KEY=       # OpenRouter/Perplexity Sonar Pro
